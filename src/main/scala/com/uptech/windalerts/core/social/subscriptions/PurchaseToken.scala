package com.uptech.windalerts.core.social.subscriptions

case class PurchaseToken(id: String,
                         userId: String,
                         purchaseToken: String,
                         creationTime: Long
                        )
